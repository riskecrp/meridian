-- Baseline marker. The live DB already matches schema.sql (regenerated
-- 2026-07-18); this migration exists only so _migrations has a starting row.
-- New migrations: migrations/NNN_short_name.sql, applied by scripts/migrate.mjs.
SELECT 1;
